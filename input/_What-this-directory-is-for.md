# Input Directory

This directory contains source documents for conversion to Markdown.

## Supported Formats

- Microsoft Word documents (`.docx`)
- PDF documents (`.pdf`)

## Usage

Place your source documents here, and they will be processed by the conversion pipeline to generate Markdown files in the `output` directory.

The output Markdown filename will match the source filename, with only the file extension changed to `.md`.
